{
  "name": "reproduce-eslint-cache-removal-bug",
  "version": "0.0.1",
  "description": "Reproduce eslint cache removal bug.",
  "private": true,
  "scripts": {
    "lint": "eslint --cache --cache-location ./.eslint-cache/ --ext .js ."
  },
  "author": "zakhar@xeneta.com",
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "eslint": "^3.17.1",
    "eslint-plugin-import": "^2.2.0"
  }
}
